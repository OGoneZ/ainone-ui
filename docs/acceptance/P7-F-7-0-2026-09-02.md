# P7 F-7-0 验收记录（Linux WebKitGTK 前置冒烟）

**日期**：2026-09-02
**状态**：✅ 通过（继续 Tailwind v4，无需降级 v3）
**基线**：`docs/plan-p7-ui.md` §F-7-0（Phase 0 阻塞项）
**执行方式**：目标 Linux 环境实测（`ssh zhubaoduo@leju`），macOS 不作为通过依据

## 环境实测结果

| 环境 | 发行版 | WebKitGTK 版本 | 结果 | 截图 |
|---|---|---|---|---|
| Ubuntu 22.04 | 22.04.5 LTS x86_64 | 2.50.4（`libwebkit2gtk-4.1-dev`） | ✅ 全通过 | `P7-0-ubuntu-webkit.png` |
| Debian 12 | 12.6（docker 容器，华为云 mirror） | 2.50.6-1~deb12u2 | ✅ 全通过 | `P7-0-debian-webkit.png` |

两环境 WebKitGTK 均 ≥ 2.44 基线（规格书 §1.4 要求）。

## 三处基线特性 → computed 值（探针实测）

| 特性 | Ubuntu 22.04 | Debian 12 | 判定 |
|---|---|---|---|
| `@property` | `rgb(56, 80, 199)` | `rgb(55, 80, 200)` | ✅ 解析生效（否则 transparent） |
| `oklch()` | `oklch(0.6 0.18 260)` | 同左 | ✅ 品牌蓝色块正确 |
| `color-mix()` | `color(srgb 0.611 0.741 0.957)` | 同左 | ✅ 浅蓝混合正确 |
| Tailwind 工具类（bg-primary + ring/50） | `rgb(57, 108, 216)` | 同左 | ✅ 工具类发射正常 |

探针：`tools/smoke-webkit.py`（WebKitWebDriver + MiniBrowser，xvfb-run 无头跑，读 `data-smoke` 元素 computed backgroundColor 断言）。入口 `src/smoke/main.tsx` + `smoke.html`（临时，收尾删除）。

## 验收判定

- **AC-P7-0-1** Ubuntu 22.04 目标机三处特性正确渲染（截图留档）→ ✅ 通过
- **AC-P7-0-2** Debian 12 同上 → ✅ 通过
- **AC-P7-0-3**（若降级才触发）→ 不适用（未降级）

## 结论

Tailwind v4 在 Ubuntu 22.04（WebKitGTK 2.50.4）与 Debian 12（2.50.6）上均正确解析 `@property`/`oklch`/`color-mix` 及 Tailwind 工具类。**继续 Tailwind v4 + shadcn/ui 方案，无需降级 v3。**
