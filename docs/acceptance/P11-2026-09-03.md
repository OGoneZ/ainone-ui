# P11 验收记录（输入交互与布局体验）

**日期**：2026-09-03
**状态**：✅ 通过（需所有者 GUI 复验的项已单独标注）
**基线**：`docs/plan-p11.md`（v1.0，含 DEC-25 fuzzysort 修订与 F-11-4a spike 结论回填）
**执行人**：Claude（开发 + vitest/cargo 单测 + bun e2e 实测）
**分支**：`main`（与 P10/P12 并行合并后基线 `dda7bd7`）

## 一、测试规模（最终）

| 层 | 数量 |
|---|---|
| vitest（纯函数 + 组件） | **271 条**（P12 合并后全仓基线；P11 净增 ~40：fuzzy 9 / atFile 10 / slash +2 / lastPrompt 10 / globalSearch 9 / GlobalSearchDialog 4） |
| Rust（cargo test） | **20 条**（`log_copy` 走 `sessions.rs` 既有模式，逻辑简单复制，借 e2e+组件 spy 覆盖） |
| e2e 探针 | `tools/spike/e2e-p11-bang-probe.ts`（F-11-4a，三 harness 实测，150s 看门狗），已登记 `run-e2e.sh` |

三连跑 vitest 稳定 271/271；cargo 20/20；`pnpm tsc --noEmit` 无错。

## 二、功能项验收（F-11-x）

| 编号 | 验收 | 结果 | 证据 |
|---|---|---|---|
| AC-P11-1 | slash 菜单键盘导航滚动跟随（页面不滚） | ✅ 逻辑 + ⚠️ GUI | `ChatPanel.tsx` scrollIntoView(block:nearest) 于 `.slash-menu` 容器内滚动；jsdom 无布局需 GUI 复验手感 |
| AC-P11-2 | `/clr`→clear 排前；子序列命中出现 | ✅ | `fuzzy.test.ts`（fuzzysort 连续/词首/起始评分）+ `slash.test.ts` 新增 2 条 |
| AC-P11-3 | 菜单弹出即高亮第 0 项，直接 Enter 选中 | ✅ | `slashHighlight` 默认 0；Enter 取 `slashMatches[slashHighlight]`；aria-selected 断言 |
| AC-P11-4 | Ctrl+F → 顶部 12% 悬浮弹层、列出最近会话 | ✅ | `GlobalSearchDialog`（cmdk CommandDialog + `.global-search-dialog` top:12% 居中）；空查询=最近 mtime 20 条 |
| AC-P11-5 | 模糊关键词命中并按相关度排序；点击/回车打开 Tab | ✅ | `filterSessions`（fuzzysort 标题主/cwd 辅）+ 组件测试：点击→onPick 收到正确 session_id |
| AC-P11-6 | Ctrl+F 不再触发会话内搜索；Ctrl+Shift+F 打开 | ✅ | ChatPanel onKeyDown 改绑 `meta+shift+f`；`ChatPanel.test.tsx` 2 条搜索用例已更新并通过 |
| AC-P11-7 | `@` 词首触发文件联想（排除 node_modules 等） | ✅ | `detectAtToken` 词首判定 + `filterExcluded` 复用；`atFile.test.ts` 10 条 |
| AC-P11-8 | `@read` fuzzy 命中 → Enter 插入 `@file:/abs` + 进胶囊 | ✅ | `applyAtToken` 替换拼装 + `pickAt` addFiles；组件链路 spy |
| AC-P11-9 | 邮箱 `a@b` 不触发；Esc 关闭 | ✅ | `detectAtToken("a@b",3)===null`；onKeyDown Esc 分支 |
| AC-P11-10 | spike 产出三 harness 判定 + 报文存档 | ✅ | `docs/protocol-samples/sample-bang-{claude-code,omp,pi}.json`；结论回填 plan-p11 §7（claude-code 执行 → 分支 a） |
| AC-P11-11 | （分支 a）`!` 透传 harness | ✅ | 按裁决：客户端透传不预执行；claude-agent-acp 下 `!cmd` 由 harness 执行（spike 实证） |
| AC-P11-12 | （分支 b 分支不适用——见 AC-P11-10 裁决）exec 超时不挂起 | ✅ N/A | 分支 b 未采纳；探针自身带 150s 看门狗已验证超时路径 |
| AC-P11-13 | 点击分叉 → 自动跳转新 Tab，父 Tab 保留 | ✅ | `onForkNavigate` → App `handleForkNavigate`（resolveHistoryOpen 语义）；flexlayout selectTab 激活 |
| AC-P11-14 | 新 Tab 历史完整回显，无「历史消息未找到」 | ✅ 逻辑 + ⚠️ GUI | Rust `log_copy`（父 JSONL 复制）+ ChatPanel 既有 logRead 回填链路；实机 fork 需 GUI |
| AC-P11-15 | 分叉失败行为不变 | ✅ | busy/无 sessionId 早退 toast，未改动 |
| AC-P11-16 | 点浮窗外自动关闭 | ✅ | document mousedown + `quickPopRef.contains` 判定；测试 spy 路径 |
| AC-P11-17 | 无「关闭」按钮；点浮窗内不关 | ✅ | 两态按钮已删除；contains 判定保证内点不触发 |
| AC-P11-18 | 右侧栏双 tab（元数据/文件），文件树不再在输入框下 | ✅ | `RightRail` 双 tab；ChatPanel 内联 FileTree 已移除；`MetadataPanel.test`/`FileTree.test` 迁移后通过 |
| AC-P11-19 | 折叠细栏杆 → 展开恢复 tab；localStorage 记忆 | ✅ | `ainone-rightrail` {open,tab}；`RightRail` 组件测试 |
| AC-P11-20 | 长会话滚动仅消息区动 | ✅ 逻辑 + ⚠️ GUI | `.container overflow:hidden`（F-11-8 壳级约束）；实机滚动手感需 GUI |
| AC-P11-21 | body 无滚动条；720×480 不破版 | ⚠️ GUI | CSS 约束已落；窗口尺寸实测需 GUI（P10 minSize 配置已有） |
| AC-P11-22 | 上滚出现「你最后说的」折叠气泡；贴底消失 | ✅ | `lastUserIndex`/`shouldShowLastPromptBubble`（64px 阈值）+ raf 节流 scroll 监听 |
| AC-P11-23 | 点击气泡 scrollToIndex 回跳 | ✅ | `jumpToLastPrompt` → virtualizer.scrollToIndex(align:start)；`lastPrompt.test.ts` 10 条 |

## 三、需要所有者 GUI 复验清单（汇总）

1. slash/@ 菜单长列表键盘翻页手感（jsdom 无法测真实滚动）
2. 实机分叉 → 跳转 → 历史回显全链路（依赖真实 harness fork 能力）
3. 长会话（50+ 条）滚动时侧栏/顶栏/输入区固定
4. Ctrl+F 全局搜索弹层观感（悬浮位置/深浅色）
5. 最小窗口 720×480 布局

## 四、变更记录

| 时间 | 内容 |
|---|---|
| 2026-09-03 | P11 九项全部实装；F-11-4 spike 实测并回填 §7；P10/P12 并行合并冲突解决（imports 双保留、diff 评论条带保留、内联 FileTree 按 P11 决策移除）；vitest 271 / cargo 20 / tsc 三绿 |
