# P21 交互体验打磨 · 验收记录

日期：2026-09-06　分支：`feat/p21-interaction`　规格书：`docs/plan-p21-interaction.md`

## 回归基线

| 项 | 结果 |
|---|---|
| `pnpm test` | **56 文件 350/350 全绿**（新增 PermCard 3 条 + isInCenterBand 4 条 + sidebarResize 7 条） |
| `cargo test` | 30/30 绿 |
| `pnpm build` / `pnpm tauri build` 前端 | 零报错 |
| `pnpm test:e2e` | 10 通过 / 0 跳过 / 0 失败 |

## AC 逐条

### AC-P21-1 tab 栏 harness logo ✓
- `onRenderTab` leading 槽挂 `AgentAvatar`（size 14），adapter 经 `tabs` 反查。
- GUI 实测（WKWebDriver，真实 WKWebView）：`.flexlayout__tab_button_leading` 在 tab 按钮与 `tab_button_stamp`（溢出菜单同款标题）均渲染出头像节点；无 logo harness 走 monogram 降级链。

### AC-P21-2 排队按钮移除 ✓
- `queueBtnGone=true`（GUI 实测无 `aria-label="排队发送"` 按钮）；busy 自动入队语义（P16 DEC-50）不变；QueueDock「立即发」不受影响；placeholder 文案改为「运行中，发送将加入队列」。

### AC-P21-3 队列拖拽中心区判定 + 吸入预览 ✓（几何单测锁定 + 动画代码评审；拖拽手势 jsdom 不可测，沿用 DEC-50 约定）
- `isInCenterBand` 纯函数 4 条单测（含浮点容差与零宽兜底——测试数据算术错误曾致 3 条假失败，已修）。
- 双条件判定：600ms 悬停 ∧ 指针在目标中心 50% 带；边缘带显示主色插入指示线（`.insert-over::before`）。
- 候选态序号位替换 `MergeIcon`；吸入动画改 Web Animations API 两段 keyframe（overlay 飞向目标中心 + scale 0.35 + fade，320ms ease-in，`fill: forwards`，finish 回调落账 merge）；无几何环境回退旧 CSS 路径。

### AC-P21-4 权限审批内嵌卡 + 侧栏光晕 ✓
- store `pending: string` → `perm: { title, options[] }`（sessionStatus 纯函数与单测同步迁移）；决策按 harness optionId 原样回传（L12 kind 前缀猜测废弃）；H10 兜底改按 reject 类选项收口。
- `PermCard`（AskCard 同款悬浮卡）渲染于 composer-dock 内；样式 `.perm-card` 注入确认（GUI 实测 cssRules 命中）；全屏权限 Dialog 移除（GUI 实测无 `[role=dialog]`）。
- 侧栏光晕：`awaiting-glow` keyframes + `.history-item.status-awaiting_input` 动画确认注入（keyframes 名与选择器双查通过）；reduced-motion 静态兜底。

### AC-P21-5 全屏编辑限定窗格 ✓（GUI 实测）
- 点「全屏编辑」后 `.composer-expanded` 位于 `.panel` 内（`inPanel=true`、`bodyChild=false`），`position: absolute`，宽高 = 窗格实际尺寸（246×465 单窗格实测）。
- Esc 退出后输入区恢复（`expandedGone=true, composerBack=true`）。

### AC-P21-6 侧栏拖宽把手 ✓（GUI 实测）
- 左栏右缘把手真实拖拽：pointer 序列一次拖动 240→300，inline style 与 `localStorage["ainone-sidebar-width"]=300` 同步；重载后读回 240（恢复现场时验证持久化读回闭环）。
- 右栏把手渲染确认（`.rightrail > .sidebar-resize-handle`）；折叠态（`sidebar-collapsed`/`rightrail-collapsed`）不渲染把手。
- 方向语义/clamp/视窗上限由 `lib/sidebarResize.ts` 7 条单测锁定。

### AC-P21-7 回归 ✓
见「回归基线」表。

## GUI 冒烟环境备注

- 主仓 dev 实例占用 1420/4445，worktree 实例改用 1421（vite）+ `TAURI_WEBDRIVER_PORT=4446 cargo run --features webdriver`（`cargo run` 必须显式带 `--features webdriver`，否则插件不编入——P19c 已知约束）。
- 期间误杀过主 worktree 的 dev 实例（pkill 匹配过宽），已用同命令恢复；4446 上残留过两个 webdriver bind 失败的僵尸实例，已清理。
- 冒烟后现场：sidebar 宽度 240、无残留全屏态、无未关闭 dialog。

## 提交时间线

| commit | 内容 |
|---|---|
| `90dd890` (main) | 规格书 docs/plan-p21-interaction.md |
| `ac88c9b` (main) | S0 遗留修复落账（H12/H8/H14 + ASR 配置化） |
| `4944594` | S1 移除排队按钮（F-21-2） |
| `3b285d3` | S2 tab logo（F-21-1） |
| `fd168c8` | S3 审批内嵌卡 + 侧栏光晕（F-21-4） |
| `5982c22` | S4 队列中心区判定 + 吸入动画（F-21-3） |
| `f04ffcd` | S5 全屏编辑窗格化（F-21-5） |
| `6d04c52` | S6 侧栏拖宽把手（F-21-6） |
