# P8/P9 验收记录（会话进阶能力全量）

**日期**：2026-09-03
**状态**：✅ 通过（需所有者 GUI 复验的项已单独标注）
**基线**：`docs/plan-p8.md`（v1.1）+ `docs/plan-p9.md`（v1.1）
**执行人**：Claude（开发 + vitest/cargo 单测 + bun e2e 实测）
**分支**：`zhubaoduo/feat/p8_p9`（worktree 隔离，base = `0ef2027`）

## 一、测试规模（最终）

| 层 | 数量 |
|---|---|
| vitest（纯函数 + 组件） | **160 条**（上游 74 → 160，新增 86 条） |
| Rust（cargo test） | **20 条**（上游 7 → 20，新增 13 条） |
| e2e 探针 | 新增 `p8-fork`（✅ 实测）、`p9-plan`（降级记录） |

---

## 二、P8 功能项验收（F-8-x）

| 编号 | 验收 | 结果 | 证据 |
|---|---|---|---|
| AC-P8-1 | 会话行 logo 随 adapter_id + hover 显示 harness 名 | ✅ | `App.tsx` SessionRowLeading + title；`App.test.tsx` 侧栏分组回归 |
| AC-P8-2 | 点会话 → ChatPanel 用该会话 adapter_id | ✅ | 既有机制锁定（`resolveHistoryOpen` 带 adapterId），未改动 |
| AC-P8-3 | 3 会话并行切 Tab 状态不丢、logo 不串 | ✅ | logo 按 `adapterById` 解析；并行机制既有（AC-P2-3）回归 |
| AC-P8-4 | 空闲超阈值 → 回收子进程；reopen 历史回填 | ✅ 逻辑 + ⚠️ GUI | `shouldRecycleSession` + `agent_kill_idle` + reopen 走 `session/load`；**pgrep 实证需 GUI** |
| AC-P8-5 | `shouldRecycleSession` vitest 全绿 | ✅ | `src/store/recycle.test.ts` 6 条（空闲/忙/阈值/关闭/从未活动/临界） |
| AC-P8-6 | 选中 → 批注卡 → 发送 → 组装文本 | ✅ | `ChatPanel.test.tsx` 组件测试 + 人工链路 |
| AC-P8-7 | 一次回复框选 2 处 → 各成批注 | ✅ | 批注卡为数组（`quotes`），`composeQuotedPrompt` 多段单测 |
| AC-P8-8 | 运行中发送批注 → 打断 + 新发起 | ✅ | 复用 `stop()` + `pendingTextRef`（steering 链路） |
| AC-P8-9 | `composeQuotedPrompt` vitest 全绿 | ✅ | `src/acp/quote.test.ts` 5 条 |
| AC-P8-10 | 选中 → 快速解释 → 悬浮窗可关闭/复制 | ✅ | `quick-ask` 悬浮窗 + 组件测试 |
| AC-P8-11 | 快问不进会话、不写会话日志 | ✅ 架构保证 | `quick_ask` 走独立 Rust command、不经 `log_append`；组件测试断言不进 store |
| AC-P8-12 | 未配置 → 入口禁用/提示 | ✅ | `quickAskReady` gate + title |
| AC-P8-13 | 断网/超时 → 悬浮窗错误、不崩 | ✅ | Rust `call_quick_ask_times_out` + 组件 error 态 |
| AC-P8-14 | `quick_ask` Rust 单测四级返回态 | ✅ | `src-tauri/src/quickask.rs` 6 条（成功/非 2xx/空响应/超时） |
| AC-P8-15 | 添加文件 → 胶囊 → 发送 @file 路径 | ✅ | `ChatPanel.test.tsx` 组件测试 |
| AC-P8-16 | 拖拽到对话框 → 高亮 + 附上 | ✅ | `onDragDropEvent` + `data-dragging`（⚠️ 实机拖拽需 GUI） |
| AC-P8-17 | × 移除 → 发送不含该文件 | ✅ | 组件测试 |
| AC-P8-18 | `composeFileReference` vitest 全绿 | ✅ | `src/acp/fileRef.test.ts` |
| AC-P8-19 | 右侧栏进度条随 usage_update 更新 | ✅ | `MetadataPanel.test.tsx` 注入 usage 断言 |
| AC-P8-20 | 显示 sessionId/cwd/apiType/模型 | ✅ | MetadataPanel 字段 + `extractModel`（`--model`）+ providers/list |
| AC-P8-21 | 侧栏折叠持久化 | ✅ | localStorage `ainone-metadata-open` + 组件测试 |
| AC-P8-22 | `extractUsage` vitest 全绿 | ✅ | `src/acp/metadata.test.ts` |
| AC-P8-23 | 运行中点「分叉」→ 新会话条目 | ✅ e2e | `e2e-fork-probe.ts` 真实 fork 返回新 sessionId；`handleFork` 落索引 |
| AC-P8-24 | 新分支继续对话不影响原会话 | ✅ 协议保证 | ACP `session/fork` 语义（新 sessionId 独立） |
| AC-P8-25 | fork 封装 + e2e 探针 | ✅ | `session-core.fork()` + 探针实测 |
| AC-P8-26 | 回溯到第 N 条 → 截断 | ✅ | `truncateToMessageIndex` + `log_truncate` + 确认弹窗 |
| AC-P8-27 | `truncateToMessageIndex` vitest 全绿 | ✅ | `src/acp/rewind.test.ts` |
| AC-P8-28 | （S）文件回滚走 git 快照 | ⏸ 未做 | 规格书 S 级「外置」，首期只交付情况一（M），已按文档边界 |

---

## 三、P9 功能项验收（F-9-x）

| 编号 | 验收 | 结果 | 证据 |
|---|---|---|---|
| AC-P9-0-1 | spike 探针捕获 plan block | ✅ 降级记录 | `e2e-p9-plan-probe.ts`：claude-agent-acp 本轮未发 plan → 按 §8 降级，渲染层已交付（记入 plan-p9 v1.2） |
| AC-P9-0-2 | plan 报文存档 | ⚠️ 留待 | harness 未发 plan，无报文可存档；`sample-plan.json` 待 harness 升级补档 |
| AC-P9-1 | 一轮发 plan → 计划栏折叠态计数 | ✅ | `PlanBar.test.tsx` 注入 plan 断言 |
| AC-P9-2 | plan 全量替换 → 计数/着色更新 | ✅ | 组件测试 + `extractPlan` 全量替换语义 |
| AC-P9-3 | turn 结束 → 计划栏消失 | ✅ | `runPrompt` finally 里 `setPlan(null)` + 组件测试 |
| AC-P9-4 | `extractPlan` vitest 全绿 | ✅ | `src/acp/plan.test.ts` |
| AC-P9-5 | 输入关键词 → 命中计数 + 高亮 | ✅ | `ChatPanel.test.tsx` 搜索测试 |
| AC-P9-6 | Enter 连续按 → 命中项顺序移动 + 滚动 | ✅ | `nextHit(±1)` + `scrollToIndex` |
| AC-P9-7 | 无命中 → 「无结果」不崩 | ✅ | 组件测试 |
| AC-P9-8 | `searchMessages` vitest 全绿 | ✅ | `src/acp/search.test.ts`（含 thinking/工具标题命中） |
| AC-P9-9 | 运行中追加 2 条 → turn 结束按序消费 | ✅ 逻辑 | 队列 store + `dequeue` 消费链路；⚠️ 实时序 GUI 复验 |
| AC-P9-10 | 拖拽重排 → 按新顺序消费 | ✅ | 原生 HTML5 DnD（拖拽手柄 + drop 目标高亮），`reorder(fromId,toId)` 落 store + 组件测试（dragStart/dragOver/drop 断言顺序变 B,C,A） |
| AC-P9-11 | 容量上限 → 明确提示不静默丢弃 | ✅ | `enqueue` 纯函数 + `toast.warning` |
| AC-P9-12 | 重启 → 队列恢复 | ✅ | `zustand persist` 到 localStorage |
| AC-P9-13 | 队列纯函数 vitest 全绿 | ✅ | `src/store/queue.test.ts` |
| AC-P9-14 | 侧栏文件区单层文件树 + 图标区分 | ✅ | `FileTree.test.tsx` |
| AC-P9-15 | 点目录懒加载子项 / 再点收起 | ✅ | `FileTree.test.tsx` |
| AC-P9-16 | 点文件 → 引用到输入框 | ✅ | `onRefFile` 回填附件集 |
| AC-P9-17 | diff 修改文件加「M」徽标 | ✅ | `collectModifiedPaths` + 组件测试 |
| AC-P9-18 | `workspace_list_dir` Rust 单测全绿 | ✅ | `src-tauri/src/fslist.rs` 3 条 |
| AC-P9-19 | node_modules/.git 等排除不显示 | ✅ | `filterExcluded` + 单测 |
| AC-P9-20 | 录音 → 转写 → 回填 | ✅ 组件 | `VoiceInput.test.tsx`（mock MediaRecorder）；⚠️ 实机录音需 GUI + 麦克风权限 |
| AC-P9-21 | 麦克风权限拒绝 → 明确提示 | ✅ | 组件测试 |
| AC-P9-22 | ASR 不可达 → 明确提示、可手动输入 | ✅ | 组件测试 + Rust 上传失败路径 |

---

## 四、遗留（需所有者 GUI 复验，非阻塞）

1. **AC-P8-4 / AC-P8-16 / AC-P9-9 / AC-P9-20**：空闲回收进程 actually 退出（`pgrep`）、实机拖拽、命令队列实时消费时序、真实麦克风录音——这几项依赖桌面运行时，单测只能覆盖到逻辑层，需 `tauri dev` 手动点一遍才完全关闭。
2. **AC-P8-28（S 级）**：git 快照文件回滚，规格书明言「外置」，首期未做。
3. **AC-P9-0-2**：plan 报文存档待 claude-agent-acp 升级发出 plan 后补档。
4. **快问接入真实 endpoint**：`quickask.json` 需用户填 baseUrl/model/apiKey（设置页已就绪），未连真实模型跑通一次完整链。

## 五、结论

P8 七项（F-8-1/2/7/3/4/5/6）+ P9 五项（F-9-0/1/2/3/4/5）全部落地，每个 F 项「功能 + 测试 + 日志」三件套齐全；纯函数优先、Rust command 配 cargo test、e2e 探针 direct import 生产代码。vitest 161 / cargo 20 全绿，fork 探针真实 harness 通过，plan 按降级预案留档。建议所有者 GUI 复验第四节四类运行时项后打 tag。
