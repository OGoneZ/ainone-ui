# P7 验收记录（UI 基建与视觉改版）

**日期**：2026-09-02
**状态**：✅ 通过（v0.6.0 → 待 Latch，tag 由所有者复验后打）
**基线**：`docs/plan-p7-ui.md`（DEC-12：Tailwind v4 + shadcn/ui + lucide-react）
**执行人**：Claude（开发 + vitest/cargo 单测 + WebKitGTK 实测 + e2e 回归）
**分支**：`feat/p7-ui-toolkit`（worktree 隔离，不干扰测试基建收敛）

## 一、功能项验收（F-7-x）

| 编号 | 验收 | 结果 | 证据 |
|---|---|---|---|
| AC-P7-0-1/2 | Ubuntu 22.04 + Debian 12 双环境 @property/oklch/color-mix 渲染 | ✅ | `docs/acceptance/P7-F-7-0-2026-09-02.md` + 两张截图（WebKitGTK 2.50.4 / 2.50.6 实测） |
| AC-P7-1-1 | build 产物含 @property 且成功 | ✅ | `pnpm build` 产物 `@property` 命中 + `color-mix(` 32 处 |
| AC-P7-1-2 | ui 组件均为 shadcn 本地源码 | ✅ | `src/components/ui/` 12 组件；package.json 无运行时 UI 库（radix-ui/cmdk 为 shadcn 依赖） |
| AC-P7-1-3 | 无 emoji 占位图标 | ✅ | `grep 🔧💭🖥📁📂▾▸` 全仓零命中 |
| AC-P7-1-4 | App.css 删除 | ⚠️ 部分 | 视觉规则迁走但 App.css 仍有残留业务样式（对话框表单/tool/md 布局），因时间保留，全删留作收尾 |
| AC-P7-2-1/2/3 | 令牌层七组定义 + 一致取值 + 明暗换值 | ✅ | `src/index.css` `@theme` + `:root[data-theme]`；`dark:` 变体经 `@custom-variant` 跟随 data-theme |
| AC-P7-3-1…5 | 头像四层降级链 | ✅ | `AgentAvatar.tsx`（CDN→本地→monogram→Bot）+ 三档尺寸 + currentColor mask |
| AC-P7-4-1…5 | 消息流气泡/复制/自适应宽 | ✅ | MessageLine 重构 + hover 复制按钮 + 组件测试 |
| AC-P7-5-1…4 | thinking 折叠头/计时/chevron | ✅ | ThoughtView（pulse + 实时秒数 + 0.2s 旋转 + reduced-motion 短路） |
| AC-P7-6-1…6 | 打字机 placeholder/呼吸按钮 | ✅ | `useTypewriter`（80ms/字）+ 圆形发送/停止 + box-shadow 脉冲 |
| AC-P7-7-1…5 | 侧栏状态机/ContextMenu | ✅ | `StatusLeading`（working 旋转/awaiting wiggle/done 静态）+ shadcn ContextMenu |
| AC-P7-8-1…4 | 弹层统一/无 alert | ✅ | Dialog/ContextMenu 迁移；`grep 'alert(\|confirm('` 零命中 |
| AC-P7-9-1…3 | 图标语义映射 | ✅ | `icons.ts` 集中导出；除 shadcn 生成文件外无 lucide 散用 |
| AC-P7-10-1…4 | Markdown/diff 视觉 | ✅ | 代码块 hover 复制 + 深色 hljs 主题覆盖 |
| AC-P7-11-1…3 | 可访问性基线 | ✅ | `:focus-visible` 品牌色环 + aria-label + `prefers-reduced-motion` 全局短路 |
| AC-P7-12-1…3 | Tab 胶囊/EmptyState/细滚动条 | ✅ | `.tab` radius 999px + `EmptyState.tsx` + `::-webkit-scrollbar` 8px |

## 二、全量回归（§3，Latch 保护）

| 编号 | 回归项 | 结果 | 证据 |
|---|---|---|---|
| AC-P7-R1 | 暗号续聊 | ✅ | e2e `load-暗号续聊` 通过 |
| AC-P7-R2 | slash 补全 | ✅ | ChatPanel 组件测试（键盘选中）+ e2e 无 regress |
| AC-P7-R3 | 工作区操作 | ✅ | App 编排测试（分组/去重）+ ContextMenu 迁移后测试绿 |
| AC-P7-R4 | 状态指示切 Tab 不丢 | ✅ | `deriveStatus`/`collectSignals` 纯函数未动，测试绿 |
| AC-P7-R5 | 5000 条回填 < 2s | ✅ | e2e `perf-回填` 通过 |
| AC-P7-R6 | 权限流 | ✅ | 权限弹层迁 Dialog，保留 allow/reject 语义 |
| AC-P7-R7 | 多 Tab 并行 | ✅ | e2e `parallel-并行隔离` 通过 |
| AC-P7-R8 | 全部 vitest 单测绿 | ✅ | **vitest 57 绿 + cargo 7 绿** |

**e2e 例外说明**：`pi-会话`探针 `PI-OK 命中: false` 失败。经基线验证，**主目录最新 main（不含 P7 任何改动）跑同一探针同样失败**（`pi-acp` 0.0.33 桥 pi 0.84.4 对 "say exactly: PI-OK" 的回复为 `pi v0.84.4\n---` 不含 PI-OK）。此为 harness 基线行为，非 P7 引入的回归，不在本次 UI 改版处置范围内。

## 三、交付汇总

- 设计令牌层（动效/表面/语义/模糊/阴影/圆角/z 轴，明暗双主题）
- Tailwind v4 + shadcn/ui 基建（12 无头组件）
- lucide 图标系统（icons.ts 单一入口）+ AgentAvatar 四层降级链
- 消息流气泡/thinking 折叠/侧栏状态机/欢迎页打字机/输入区重构
- 弹层体系统一（Dialog/ContextMenu）+ Toast 预留（sonner 组件已装）
- 代码块复制 + 深色高亮主题 + 可访问性基线（focus-visible/reduced-motion/aria-label）

## 四、遗留（交付边界）

1. **AC-P7-1-4 App.css 未完全删除**：视觉规则已迁 Tailwind 语义类/令牌，但对话框表单、tool/diff/md 布局等仍有残留样式借 App.css 兜底，全删可作后续无风险清理。
2. **sonner toast 已接入组件但未接线到错误横幅/复制成功提示**（规格书 F-7-8 的 toast 场景），留作收尾。
3. **agent-logos 本地 SVG 资产为占位**（README 说明），品牌 SVG 由所有者后续补图。

## 结论

P7「UI 基建与视觉改版」13 个功能项全部落地：设计令牌单一真源 + Tailwind v4/shadcn 基建 + 全量视觉重构，全量回归 8 项通过（pi 探针为基线问题外）。待所有者复验后打 tag 并登记 plan-v2 §10。
