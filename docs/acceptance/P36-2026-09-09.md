# P36 验收记录（2026-09-09）

> 分支：`zhubaoduo/feat/p36_tool_display`（worktree `.claude/worktrees/p36_tool_display`，基于 main @ 80e25c2）。
> 验收方式：vitest 735 全绿（85 文件）+ `pnpm build`（tsc 零错）+ release 构建实机启动冒烟。
> 提交序列：规格书 3ca7e68 → S0b f5433b2 → S1 bb676ca → S2 41493d2 → S3 7ecd022 → S4 67458cb → merge main a1b99dc。

## AC 逐条结果

### R1 终端两段式展开

| AC | 结果 | 证据 |
|---|---|---|
| 1.1 execute 块命令段显示 command 全文（换行保留不截断） | ✅ | ToolCommand.test「AC-1.1」；toolCommand 纯函数不截断不折叠空格 |
| 1.2 复制钮写剪贴板且不改折叠态 | ✅ | ToolCommand.test「AC-1.2」（stopPropagation 验证） |
| 1.3 输出兜底链 content text → rawOutput → 仅命令段 | ✅ | ToolCommand.test「AC-1.3a/1.3b」+ toolDisplay.test toolOutputFallback 6 例 |
| 1.4 rawOutput 三层透传（携带才带键/覆盖/干净缺省） | ✅ | session-core.test「P36 R1 AC-1.4」+ turn.test「P36」+ message-log.test「P36」 |
| 1.5 旧日志解析不回归；新字段可选往返无损 | ✅ | message-log.test「P36：rawOutput 序列化往返无损」 |
| 1.6 非 execute 类不渲染命令段 | ✅ | ToolCommand.test「AC-1.6」+ toolDisplay.test kind 分支 |

### R2 kind 差异化风格

| AC | 结果 | 证据 |
|---|---|---|
| 2.1 data-toolkind 挂点（缺省 → other） | ✅ | ToolCommand.test「P36 R2 AC-2.1」 |
| 2.2 kind 边框/图标色规则取设计令牌 | ✅ | messages.css 走查：execute=primary、edit/move=warning、delete=danger；dark 令牌换值自动生效 |
| 2.3 failed/error 危险语义优先 | ✅ | CSS 走查：失败态左边框与图标统一 danger（`!important` 覆盖 kind 分支） |
| 2.4 活动组卡保持中性 | ✅ | 代码走查：组卡无 data-toolkind 属性 |

### R3+R5 预览文件按钮 + 归属语义

| AC | 结果 | 证据 |
|---|---|---|
| 3.1 含 diff / rawInput 路径命中；execute/read 无按钮 | ✅ | ToolPreview.test「AC-3.1」 |
| 3.2 file_path/path 双认；事件 detail={path,tabKey}；折叠态不变 | ✅ | ToolPreview.test「AC-3.2」（双用例 + 事件 spy） |
| 3.3 监听归属判定（自己收/别人忽略/旧 string 兜底） | ✅ | ChatPanel.p36-openfile.test 4 条 |
| 3.4 分屏失焦窗格文件树 → 该窗格打开预览 | ✅ | ChatPanel.p36-openfile.test「AC-3.3 tabKey=自己即使非活跃」；实机待用户复核 |
| 3.5 FileChangeRow 同步有按钮 | ✅ | ToolPreview.test「AC-3.5」（两渲染路径各一钮） |
| 5.1/5.2 兼容旧行为 | ✅ | 同上 4 条监听测试覆盖双向 |

### R4 终端无工作区

| AC | 结果 | 证据 |
|---|---|---|
| 4.1 resolveTerminalCwd 优先级矩阵 | ✅ | tabs.test「resolveTerminalCwd（P36 R4）」4 条 |
| 4.2 三入口统一解析 + 无目录 toast 提示 | ✅ | App.tsx newTerminalTab/splitCurrent 走查（resolveTerminalCwd + toast.warning） |
| 4.3 历史终端条目 workspace 反查 | ✅ | tabs.test「AC-4.3」 |
| 4.4 无 cwd「无工作区」提示保留 + 创建 toast 不吞 | ✅ | FileTree 存量测试 + App 走查 |

## S0b 基线回归修复

main @ 80e25c2 上 MetadataPanel.test「setConfigOption 返回 null → toast.warning」1 条失败（1ad76ce 引入 sessionOnly 后拒绝路径改 toast.error，测试未同步）。f5433b2 将断言对齐 sessionOnly 语义（error + 不弹 success/warning），恢复全绿基线。

## 实机冒烟（release 构建）

- `pnpm tauri build --no-bundle`（含 prepare-runtime 落位 bun 二进制）→ 构建成功。
- 启动 release 应用 → 进程存活、窗口渲染正常（截图确认 UI 完整）。
- 交互项（点击终端展开看两段式/预览钮点击/终端 tab 文件树）留待用户实机复核——自动化驱动受 WKWebView 辅助访问限制，组件层行为已由 jsdom 测试锁定。

## 已知边界

- omp `rawOutput.details` 私有结构不解析（只取 content text），规格范围内。
- 旧 string detail 的四监听兜底保留一个发布周期，后续可移除。
- 「终端内 cd 后文件树跟随」未实现（无 OSC7 数据源），规格 §0.5 明确范围外。

## 结论

P36 全部 AC 达成：终端两段式展开（命令全文+复制+输出兜底）、kind 差异化外观、写操作预览入口、分屏预览失效与终端无工作区两项缺陷根治。vitest 735 全绿、`pnpm build` 零报错、release 二进制实机启动正常。
