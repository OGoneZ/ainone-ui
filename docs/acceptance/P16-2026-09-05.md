# P16 验收记录（侧栏预览 / 消息锚点 / 队列悬浮重构）

**日期**：2026-09-05
**分支**：`zhubaoduo/feat/p16_rail_queue`（基线 main `ff528c3`，规格书 P16 v1.0）
**规格书**：docs/plan-p16.md（DEC-48..51）

## 提交链

| 提交 | 内容 |
|---|---|
| 1c15d8a | chore：引入 @dnd-kit/core+sortable+utilities+modifiers 与 shiki；tauri.conf 开 assetProtocol |
| c694fc3 | fix：Cargo.toml 补 tauri `protocol-asset` feature（assetProtocol 生效前提） |
| 88cf57c | feat：F-16-1 文件预览（previewKind + FilePreview + FileTree 单击 + open-file 事件链） |
| de4e283 | feat：F-16-2 历史 tab（historyAnchors + HistoryPanel + 三 tab + jump/rewind 接线） |
| d2696d4 | feat：F-16-3 队列悬浮 Dock（dnd-kit 挤位/合并 + busy 入队 + 删 CommandQueuePanel） |

## 自动化回归（全绿）

| 门禁 | 结果 |
|---|---|
| vitest | **53 文件 / 313 测试全过**（基线 49/275，新增 4 文件 38 测试；删 CommandQueuePanel.test 5 条被 QueueDock.test 6 条替代） |
| cargo | 28 passed / 0 failed |
| e2e | 10 通过 / 0 跳过 / 0 失败 |
| tsc --noEmit | 零错误 |
| vite build | 成功 |

## AC 逐条

| AC | 结论 | 证据 |
|---|---|---|
| AC-P16-1 预览分流渲染 | ✅ 代码级达成 | previewKind.test 12 条映射断言；FilePreview.test 7 条（code/markdown/text/image/binary 分流、×/Esc 关闭） |
| AC-P16-2 多窗格独立 + active 守卫 | ✅ | open-file 事件监听带 `activeRef` 守卫（同 ref-file M5 模式）；previewPath 为 ChatPanel per-tab state，窗格独立 |
| AC-P16-3 >1MB 提示 + 目录不触发 | ✅ | FilePreview.test too-large 用例（PREVIEW_TEXT_LIMIT=1_000_000）；FileTree.test 目录行不触发 onOpenFile |
| AC-P16-4 历史 tab + 跳转 | ✅ | historyAnchors.test 6 条；HistoryPanel.test 5 条（列表/事件/空态）；jumpToIndex 复用双 rAF 校跳（自 jumpToLastPrompt 抽出，语义逐字保持） |
| AC-P16-5 历史回溯 | ✅ | rewind-request 事件 → ChatPanel `setRewindTarget(index)` → 复用既有确认 Dialog + doRewind busy 保护 + logTruncate 链路 |
| AC-P16-6 队列浮层两态 | ✅ | QueueDock.test：空队列不渲染/徽标/展开/编辑/删除 |
| AC-P16-7 拖拽挤位 | ✅（代码级） | dnd-kit sortable verticalListSortingStrategy 自带 transform transition；DragOverlay + ghost 半透明。真实手感需 GUI 手测 |
| AC-P16-8 合并动画与语义 | ✅（代码级） | mergeItems 纯函数 5 条单测（向下/向上/跨位/自合/长度减一）；悬停 600ms → candidate 脉冲 → 松手 320ms 吸入 → store.merge 落账；jsdom 不做手势断言 |
| AC-P16-9 busy 入队 + 立即发 + 停止保留 | ✅ | submit busy 分支改 enqueueCommand（toast 第 N 位）；sendSuggestion/sendQuotes/sendDiffComments 三处同步改入队；sendNowSteer 保 M2 先赋值再 stop + 先移出队列防重复；M1 cancelled/user 不消费逐字保留 |
| AC-P16-10 旧实现清除 + 无双套拖拽 | ✅ | CommandQueuePanel.tsx/.test 已删；.queue-panel 系样式已删（chat.css grep 仅剩迁移注释）；dataTransfer 残留仅 flexlayout 外拖接入（P15 DEC-43，属窗口布局域） |
| AC-P16-11 行为零回归 | ✅ | 上表四门禁全绿；ChatPanel.test / p12.test / MessageMemo.test 未放宽 |
| AC-P16-12 依赖面 | ✅ | 仅新增 @dnd-kit/*（4 件，含 modifiers）+ shiki；queueStore skipHydration 注释保留 |

## 偏差与说明

1. **@dnd-kit/modifiers 多引入一件**（规格书 §2.2 写三件）：restrictToVerticalAxis 在 modifiers 包而非 sortable，属 API 事实修正。
2. **AC-P16-7/8 动画为代码级达成**：dnd-kit 挤位过渡与 CSS 吸入动画逻辑均已实现，但 jsdom 无法模拟真实指针手势，最终手感以 GUI 冒烟为准（见下「待手测」）。
3. **消息树呈现形态**：经用户确认（AskUserQuestion）取「当前分支扁平列表」方案——消息模型无线性树结构，不伪造树形缩进；回溯=剪枝后锚点自动变短。
4. **pendingTextRef 残留消费方**：sendNowSteer（立即发）与编辑重试 busy 分支保留 steering 链路，M2 语义未删。

## 待 GUI 手测清单（本次自动化未覆盖）

- [ ] 预览浮层在 Tauri 实窗内滚动/输入不受阻；分屏两窗格各自独立开合
- [ ] 图片 asset protocol 实际加载（jsdom 不走真内核）
- [ ] 拖拽挤位过渡手感 + 合并吸入动画观感
- [ ] 历史跳转在 1000+ 条消息会话中的定位准确性
- [ ] busy 入队 toast「已加入队列（第 N 位）」文案
