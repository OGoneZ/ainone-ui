# P29 验收记录：分发就绪——一键补齐 Harness + 认证态探测 + 配置文件合并写 + 设置页卡片化

> 日期：2026-09-07
> 规格：`docs/plan-p29-onboarding.md`
> 执行：main 分支小步提交（7f314ce → 78455c7）

## 验收结果总览

| 任务 | 验收项 | 结果 | 说明 |
|---|---|---|---|
| 一：CLI 一键安装 | 1.1 cli_status 纯函数候选判定 | ✅ | `candidate_availability_matrix` / `cli_installable_any_candidate_hits` |
| | 1.2 四态 snake_case 序列化 | ✅ | `state_serializes_four_states` |
| | 1.3 claude 实机安装 | ⏸ | 候选链 URL/落点已实机核实（`~/.local/bin/claude` symlink 现存）；真装流程需先卸载本机 claude，影响工作环境，改由用户首次使用时自然验证 |
| | 1.4 CLI 装完桥失败可续补 | ✅ | `install_cli` 失败路径无任何清理（代码走查）；cli_installed 标志 + adapterRef 刷新串联见向导测试 |
| | 1.5 装桥失败清半成品不回归 | ✅ | 存量 `resolve_in_*` 全绿 |
| | 1.6 并发安装串行化 | ✅ | 与桥安装共用 `install_lock` |
| 二：认证态探测 | 2.1 探测矩阵 | ✅ | `claude_credentials_detects_oauth_shapes` / `codex_auth_three_states` / `nonempty_auth_json_matrix` / `probe_auth_home_injection_matrix` |
| | 2.2 实机快照 | ✅ | `real_machine_auth_snapshot`：本机 claude=Api、codex=Api |
| | 2.3 密钥不回传 | ✅ | AuthInfo 仅 state+detail，结构体字段审查通过 |
| 三：配置合并写 | 3.1 claude 合并不动无关键 | ✅ | `claude_merge_keeps_unrelated_keys`（permissions/hooks/API_TIMEOUT_MS 逐断言） |
| | 3.2 env 缺失创建 | ✅ | `claude_merge_creates_env_when_missing` |
| | 3.3 损坏 JSON 报错不覆盖 | ✅ | `claude_merge_rejects_corrupt_input` |
| | 3.4 codex toml_edit 保留注释 | ✅ | `codex_merge_preserves_comments_and_existing_provider` |
| | 3.5 写前备份 | ✅ | harness_config_save 中 `copy → <file>.ainone-bak`（合并写单测覆盖文本层，落盘路径由实机使用验证） |
| | 3.6 key 留空保留 | ✅ | `claude_merge_keeps_unrelated_keys`（sk-old 保留）+ `pi_merge_keeps_existing_key_when_blank` |
| | 3.7 pi/opencode provider 增改 | ✅ | `pi_merge_*` / `opencode_merge_writes_provider_and_model` |
| | 3.8 实机 read→save 同值 diff | ⏸ | 涉及改写用户本机真实配置文件，建议用户自行操作一次验证（写前有 .ainone-bak 兜底） |
| | 3.9 codex key spawn 注入 | ✅ | `codex_key_store_and_inject_roundtrip`（env 名 AINONE_CODEX_API_KEY）；agent.rs codex-acp 分支随 BRIDGES 登记生效 |
| 四：设置页+向导 | 4.1 四态×auth 组合文案 | ✅ | `四态徽标` + `statusLine` 纯函数分叉 |
| | 4.2 串联安装调用序 | ✅ | 设置页 `cli_installable 点一键安装` + 向导 `先装 CLI → 接装桥（callOrder=["cli","bridge"]）` |
| | 4.3 CLI 成功桥失败不建会话 | ✅ | 向导 `CLI 装成功、桥装失败` |
| | 4.4 配置模型读/存/留空提示 | ✅ | 设置页 `配置模型：展开读取回显 → 保存` |
| | 4.5 预置卡片无高级字段 | ✅ | `P29 卡片化：预置卡片暴露状态徽标 + 配置模型，不暴露 id/program 输入框` |
| | 4.6 向导 cli_installable 可进并串联 | ✅ | NewSessionModal P29 4 条 |
| | 4.7 快问/语音折叠区 | ✅ | `more-toggle` 展开/收起测试 |
| | 4.8 全量回归 | ✅ | cargo 97 全绿 / vitest 519 全绿 / tsc 0 错（并行 agent 未提交文件除外） |

## 状态：**Latch（带 2 项用户侧实机复核）**

- ⏸ 1.3 / 3.8 需要动用户真实环境（卸载本机 claude / 改写真实配置文件），不宜在开发会话中代做。CLI 安装候选链（URL 200、包名 bin、落点目录、`--no-modify-path`）均已实机核实过，风险集中在「网络环境」一项。
- 已知边界：omp 的 YAML 写入为受控同构生成（不保留用户注释）；配置代写仅覆盖预置五家 id。

## 提交链

| commit | 内容 |
|---|---|
| 7f314ce | docs(p29) 规格书（候选链/包名/落点实机核实） |
| b649013 | feat(p29) CLI 一键安装 + 四态判定（CLI_INSTALLERS/cli_install/env_path 补 ~/.opencode/bin） |
| 8a09f2d | feat(p29) 认证态探测（auth 模块 + 实机快照测试） |
| cdcbc9e | feat(p29) 五家配置读与合并写（harness_config/harness_keys + toml_edit） |
| 8ec67b0 | feat(p29) 前端 IPC 层对齐（四态/cli/auth 字段 + installCli/harnessConfig 封装） |
| 8d8a835 | feat(p29) 新建会话向导接 cli_installable（两层串联 + 失败区分文案） |
| 78455c7 | feat(p29) 设置页卡片化重排（状态卡/一键安装串联/配置模型表单/更多服务折叠） |
